const card = {
    maxWidth: 300,
    minWidth: 200,
    display: 'flex',
    alignItems: 'center',
    justifyContent: 'center',
    flexDirection: 'column',
    p: 2,
    gap: 10
}

const avatar = {
    width: 100,
    height: 100,
}

const avatarLink = {
    display: 'flex',
    flexDirection: 'column',
    justifyContent: 'center',
    gap: 2
}

const addUserBtn = {
    position: 'absolute',
    top: 25
}

export default {
    card,
    avatar,
    avatarLink,
    addUserBtn
}